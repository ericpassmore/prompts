{
  "scope": 2,
  "behavior": 2,
  "dependency": 2,
  "uncertainty": 0,
  "verification": 2,
  "evidence": {
    "scope": "files=3 surface=skill+rules+script for landing metadata flow",
    "behavior": "single lifecycle insertion with one new helper script",
    "dependency": "interfaces=git plumbing + existing manifest schema",
    "uncertainty": "behavior is deterministic and local to repository workflows",
    "verification": "checks=shell syntax + targeted grep + stage validators",
    "guardrails": "no external deps, no schema migration, reversible with script/rule rollback"
  },
  "guardrails": {
    "no_schema_or_api_contract_change": true,
    "no_new_external_dependencies": true,
    "localized_surface": false,
    "reversible_with_straightforward_verification": true
  },
  "overrides": {
    "reason": ""
  }
}
